export const SERVER = {
  PORT: Number(process.env.PORT) || 3000,
};

export const DOWNLOAD = {
  DEFAULT_CONCURRENT_WORKERS: 4,
  DEFAULT_DOWNLOAD_PATH: "./downloads",
};

export const API = {
  PREFIX: "/api",
};

export const APP = {
  NAME: "Universal Social Downloader",
  VERSION: "3.0.0",
};